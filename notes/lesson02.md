## What I learned today

- I now understand the difference between Git and GitHub.
- I learned how to use git add, git commit and git push.
- I learned what variables are in Dart.
- I learned how string interpolation works.

## Challenges

I forgot to use `-m` with `git commit`, so Git thought my commit message was a filename.

I also used `Print()` instead of `print()`, which caused an error.

## How I solved it

I read the error message, corrected my syntax, and successfully ran the program.